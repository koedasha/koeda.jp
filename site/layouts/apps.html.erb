<% content_for :inner_layout_content do %>
  <nav class="app-nav">
    <h2>
      <%= image_tag(content_for(:app_image), alt: content_for(:app_name), class: "app-col__icon") %>
      <%= content_for :app_name %>
    </h2>
  </nav>
  <main>
    <%= yield %>
  </main>
<% end %>
<%= render "layouts/site" %>
